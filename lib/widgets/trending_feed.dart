import 'package:flutter/material.dart';

class TendingFeed extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Center(
      child: new ListView(
        children: <Widget>[],
      ),
    );
  }
}
